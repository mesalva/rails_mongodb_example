<%= form_for(@user_ranking) do |f| %>
  <% if @user_ranking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_ranking.errors.count, "error") %> prohibited this user_ranking from being saved:</h2>

      <ul>
      <% @user_ranking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :path %><br>
    <%= f.text_field :path %>
  </div>
  <div class="field">
    <%= f.label :points %><br>
    <%= f.number_field :points %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
